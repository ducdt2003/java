spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: stat-group
      auto-offset-reset: earliest

  rabbitmq:
    # Đổi 'rabbitmq' thành 'localhost'
    host: localhost
    port: 5672
    username: guest
    password: guest